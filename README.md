# TikTok LIVE Event Portable

Repo phân phối các bản chạy sẵn của TikTok LIVE Event Middleware (Direct mode).

> Build portable đang được tạo tự động cho Windows/Linux; Termux được đóng gói riêng để không cần `pip install`/`npm install`.
